def citycon(city,country,population = ''):
    if population:
        full_city = (country+', '+city+', '+population)
        print(fullcity.title())
    else:
        fullcity = (country+', '+city)
        print(fullcity.title())